import PageStub from '@/components/PageStub';
export default function Page() {
  return (
    <PageStub
      title="Compare"
      description="Side-by-side comparison of wallets, tokens, and entities."
      features={['Wallet vs Wallet', 'Token vs Token', 'Entity vs Entity']}
    />
  );
}
