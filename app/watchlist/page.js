import PageStub from '@/components/PageStub';
export default function Page() {
  return (
    <PageStub
      title="Watchlist"
      description="Favorite wallets, tokens, and entities with custom folders and local storage sync."
      features={['Wallets', 'Tokens', 'Entities', 'Custom folders', 'Local storage sync']}
    />
  );
}
